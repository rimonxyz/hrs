package com.moninfotech.commons.pojo;

public class FacilityWrapper {
    private ParamFacilities facilities;

    public FacilityWrapper(ParamFacilities facilities) {
        this.facilities = facilities;
    }


    public Boolean getRestaurant() {
        if (!facilities.getRestaurant()) return null;
        return facilities.getRestaurant();
    }

    public Boolean getLift() {
        if (!facilities.getLift()) return null;
        return facilities.getLift();
    }

    public Boolean getWifi() {
        if (!facilities.getWifi()) return null;
        return facilities.getWifi();
    }

    public Boolean getConferenceRoom() {
        if (!facilities.getConferenceRoom()) return null;
        return facilities.getConferenceRoom();
    }

    public Boolean getSwimingPool() {
        if (!facilities.getSwimingPool()) return null;
        return facilities.getSwimingPool();
    }

    public Boolean getSportsZone() {
        if (!facilities.getSportsZone()) return null;
        return facilities.getSportsZone();
    }

    public Boolean getGym() {
        if (!facilities.getGym()) return null;
        return facilities.getGym();
    }

    public Boolean getSpa() {
        if (!facilities.getSpa()) return null;
        return facilities.getSpa();
    }

    public Boolean getBar() {
        if (!facilities.getBar()) return null;
        return facilities.getBar();
    }

    public Boolean getHelipad() {
        if (!facilities.getHelipad()) return null;
        return facilities.getHelipad();
    }

    public Boolean getAc() {
        if (!facilities.getAc()) return null;
        return facilities.getAc();
    }

    public Boolean getFrontDesk() {
        if (!facilities.getFrontDesk()) return null;
        return facilities.getFrontDesk();
    }

    public Boolean getRoomService() {
        if (!facilities.getRoomService()) return null;
        return facilities.getRoomService();
    }

    public Boolean getElectricity() {
        if (!facilities.getElectricity()) return null;
        return facilities.getElectricity();
    }

    public Boolean getSecurity() {
        if (!facilities.getSecurity()) return null;
        return facilities.getSecurity();
    }

    public Boolean getComplementaryBreakfast() {
        if (!facilities.getComplementaryBreakfast()) return null;
        return facilities.getComplementaryBreakfast();
    }

    public Boolean getCoffeeShop() {
        if (!facilities.getCoffeeShop()) return null;
        return facilities.getCoffeeShop();
    }

    public Boolean getFreeParking() {
        if (!facilities.getFreeParking()) return null;
        return facilities.getFreeParking();
    }

    public Boolean getAirportTransportation() {
        if (!facilities.getAirportTransportation()) return null;
        return facilities.getAirportTransportation();
    }

    public Boolean getBusinessCenter() {
        if (!facilities.getBusinessCenter()) return null;
        return facilities.getBusinessCenter();
    }

    public Boolean getMeetingRoom() {
        if (!facilities.getMeetingRoom()) return null;
        return facilities.getMeetingRoom();
    }

    public Boolean getDoctorOnCall() {
        if (!facilities.getDoctorOnCall()) return null;
        return facilities.getDoctorOnCall();
    }

    public Boolean getWakeupService() {
        if (!facilities.getWakeupService()) return null;
        return facilities.getWakeupService();
    }

    public Boolean getWheelChairAccessible() {
        if (!facilities.getWheelChairAccessible()) return null;
        return facilities.getWheelChairAccessible();
    }

    public Boolean getSafeDepositBox() {
        if (!facilities.getSafeDepositBox()) return null;
        return facilities.getSafeDepositBox();
    }

    public Boolean getNonSmokingRoom() {
        if (!facilities.getNonSmokingRoom()) return null;
        return facilities.getNonSmokingRoom();
    }

    public Boolean getVipFloor() {
        if (!facilities.getVipFloor()) return null;
        return facilities.getVipFloor();
    }

    public Boolean getAcceptCreditCard() {
        if (!facilities.getAcceptCreditCard()) return null;
        return facilities.getAcceptCreditCard();
    }

    public Boolean getNewspaper() {
        if (!facilities.getNewspaper()) return null;
        return facilities.getNewspaper();
    }

    public Boolean getLaundry() {
        if (!facilities.getLaundry()) return null;
        return facilities.getLaundry();
    }

    public Boolean getLobby() {
        if (!facilities.getLobby()) return null;
        return facilities.getLobby();
    }

    public Boolean getForeignExchange() {
        if (!facilities.getForeignExchange()) return null;
        return facilities.getForeignExchange();
    }

    public Boolean getExecutiveLaunge() {
        if (!facilities.getExecutiveLaunge()) return null;
        return facilities.getExecutiveLaunge();
    }

    public Boolean getBabySitting() {
        if (!facilities.getBabySitting()) return null;
        return facilities.getBabySitting();
    }
}
